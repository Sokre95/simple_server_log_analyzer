# Simple server log parser & analyzer

## Usage

* Setup
`bundle install` or just `bundle`

* Run specs
` rspec spec/`

* Run statistics example
`ruby parse.rb -f <log-file>` or just `ruby parse.rb` ('webserver.log' is default log file)